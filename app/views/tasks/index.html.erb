<%= render "hey" %>
<%= render "estimated_time" %>

<h4 class="index">TOP PRIORITY</h3>
<ul>
  <div>
    <% @tasks.where(priority: 1).each do |task|%>
      <li><%= task.name %></li>
    <% end %>
  </div>
</ul>

<h4 class="index">TASKS</h3>
<ul>
  <div>
    <% @tasks.where(priority: [2,3]).each do |task|%>
      <li><%= task.name %></li>
    <% end %>
  </div>
</ul>
